# v1.0.0 Release Notes

Release date: 2026-02-28

## Highlights

- P0 full chain delivered: task/document upload, async processing, risk generation, report query.
- P1 delivered: export, historical comparison, rule admin backend/frontend, analytics dashboard.
- P2 delivered: Python OCR parser module, RAG retrieval pipeline, i18n for backend/frontend/export.
- Ops and governance: CI, templates, release/rollback runbooks, backup scripts, retention and compliance docs.

## Validation

- `make lint` (fallback `go vet`) passed.
- `make test` passed.
- Migration rollback drill passed (`make migrate-down && make migrate-up`).
- Gray/rollback scripts validated.

## Known limitations

- Frontend `next build` requires dependency installation in runtime environment.
- Production gray observation must be completed with real traffic monitoring platform.
